services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile.agent
    environment:
      - PROC_BASE=/host/proc
      - PID1_PROC_BASE=/host/proc/1
    ports:
      - "90:9090"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /proc:/host/proc:ro