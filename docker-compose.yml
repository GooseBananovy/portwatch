services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile.agent
    pid: host
    network_mode: host
    ports:
      - "90:9090"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock